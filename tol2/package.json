{
  "name": "tol2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "forever": "sudo forever --minUptime 3000 --spinSleepTime 3000 -a start -c 'nodemon --exitcrash --delay 10' .bin/www -o .logs/out.log -e ./logs/err.log && sleep 2 && sudo forever list",
    "kill": "killall -9 node"
  },
  "dependencies": {
    "body-parser": "~1.15.1",
    "cookie-parser": "~1.4.3",
    "cors-express": "^0.2.2",
    "debug": "~2.2.0",
    "express": "~4.13.4",
    "express-cors": "0.0.3",
    "jade": "~1.11.0",
    "morgan": "~1.7.0",
    "serve-favicon": "~2.3.0"
  }
}
