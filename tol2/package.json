{
  "name": "tol2",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "node ./bin/www",
    "debug": "DEBUG=express:* node ./bin/www",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon server.js",
    "forever": "sudo forever --minUptime 3000 --spinSleepTime 3000 -a start -c 'nodemon --exitcrash --delay 10' ./bin/www -o .logs/out.log -e ./logs/err.log && sleep 2 && sudo forever list",
    "foreverDebug": "sudo DEBUG=express:* forever --minUptime 3000 --spinSleepTime 3000 -a start -c 'nodemon --exitcrash --delay 10' ./bin/www -o .logs/out.log -e ./logs/err.log && sleep 2 && sudo forever list",
    "restart": "sudo npm run kill && sudo npm run forever",
    "restart-debug": "sudo npm run kill && sudo npm run foreverDebug",
    "kill": "killall -9 node"
  },
  "dependencies": {
    "body-parser": "~1.15.1",
    "cookie-parser": "~1.4.3",
    "cors": "^2.8.0",
    "cors-express": "^0.2.2",
    "debug": "~2.2.0",
    "express": "~4.13.4",
    "express-cors": "0.0.3",
    "jade": "~1.11.0",
    "mailchimp-api-v3": "^1.4.0",
    "morgan": "~1.7.0",
    "nodemailer": "^2.6.4",
    "nodemailer-sparkpost-transport": "^1.0.0",
    "serve-favicon": "~2.3.0",
    "type-of": "^2.0.1"
  },
  "devDependencies": {
    "forever": "^0.15.2",
    "nodemon": "^1.10.2"
  }
}
